Data/*.db
Data/*.db-shm
Data/*.db-wal
Storage/Files/
wwwroot/dist/
bin/
obj/

# Local secret overrides (use dotnet user-secrets or env vars)
appsettings.Local.json
appsettings.*.local.json
secrets.json

# IDE
.vs/
*.user

# Bản sao CSDL — mỗi bản ~30MB. Luật "Data/*.db" ở trên KHÔNG phủ thư mục con,
# nên thiếu dòng này là mỗi lượt sao lưu đẩy 30MB vào git.
Data/backups/
